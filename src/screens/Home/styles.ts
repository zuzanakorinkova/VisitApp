import { StyleSheet } from 'react-native'
import colors from '../../theme/colors'

const styles = StyleSheet.create({
    container: {
        backgroundColor: colors.colors.background, padding: 5
        
    }
})


export default styles;